directive @refreshToken on FIELD_DEFINITION
directive @auth on FIELD_DEFINITION
scalar Cursor
scalar Time
scalar Map

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: Cursor
  endCursor: Cursor
}
interface Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
}

type Mutation
